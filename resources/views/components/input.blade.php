<div class="mb-3">
    <div class="container text-danger errors row p-0"></div>
    <label for="{{ $name }}" class="form-label">{{ $text }}</label>
    <input type="{{ $type }}" class="form-control" name="{{ $name }}" id="{{ $name }}">
</div>
